-- name: AI天气预报
-- descript: 解锁尊享会员(ver. 1.2.2.1)
-- package: com.weatherforecastnetba
-- author: 怀天

hook {
  class = "com.tianqi2345.module.OooO0Oo.OooOO0",
  classloader = lpparam.classLoader,
  method = "OoooOOo",
  params = {},
  before = function(it)

  end,
  after = function(it)
    it.result = false
  end,
}hook {
  class = "com.tianqi2345.module.OooO0Oo.OooOO0",
  classloader = lpparam.classLoader,
  method = "OooooOo",
  params = {},
  before = function(it)

  end,
  after = function(it)
    it.result = true
  end,
}hook {
  class = "com.android2345.core.utils.DateUtil",
  classloader = lpparam.classLoader,
  method = "OooO0oO",
  params = {"long","String"},
  before = function(it)

  end,
  after = function(it)
    it.result = "5555-55-55"
  end,
}hook {
  class = "com.tianqi2345.module.OooO0Oo.OooOO0",
  classloader = lpparam.classLoader,
  method = "ooOO",
  params = {},
  before = function(it)

  end,
  after = function(it)
    it.result = true
  end,
}hook {
  class = "com.tianqi2345.module.OooO0Oo.OooOO0",
  classloader = lpparam.classLoader,
  method = "OoooooO",
  params = {},
  before = function(it)

  end,
  after = function(it)
    it.result = true
  end,
}
